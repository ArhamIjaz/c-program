#include <stdio.h>
int isPrime(int x);
int main(){
int n,result;
printf("enter the number");
scanf("%d",&n);
result=isPrime(n);
if(result==1){
    printf("%d is not prime",n);
}
else{
    printf("%d is prime",n);
}
return 0;
}

int isPrime( int x){
int i,flag=0;
for(i=2;i<=x;i++){
    if(x%i==o){
        flag=1;
        break;
    }
    else{
        flag=0;
    }
    return flag;
}


return
}
